<Window x:Class="Personal_Well_Being.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Personal_Well_Being"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">

            <Border Background="GhostWhite" BorderBrush="Black" BorderThickness="1">
                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <RowDefinition Height="5*"/>
                    </Grid.RowDefinitions>



                    <TextBlock FontSize="50" TextAlignment="Center" Grid.Row="0">Real Stats</TextBlock>

                    <StackPanel Grid.Row="1">
                        <Button x:Name="NewGameButton" Margin="10" Click="NewGameButton_Click">New Game</Button>
                        <Button x:Name="LoadGameButton" Margin="10" Click="LoadGameButton_Click">Load Game</Button>
                        <Button x:Name="QuitButton" Margin="10" Click="QuitButton_Click">Quit</Button>
                    </StackPanel>
                </Grid>
            </Border>
            
        </Grid>

        <Frame x:Name="Main"/>
    </Grid>
</Window>
